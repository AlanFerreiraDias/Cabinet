%h1= @doc.title
%p= @doc.content

= link_to "All Documents", documents_path
= link_to "Fix Document", edit_document_path(@doc)
= link_to "Trash It", document_path(@doc), method: :delete, data: { confirm: "Are you sure?" }
